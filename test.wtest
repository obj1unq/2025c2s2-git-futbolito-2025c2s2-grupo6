import objetos.*
describe "test fulbito" {
  test "taquito" {
    lionel.taquito()
    lionel.position(pelota.position())
    lionel.taquito()
    lionel.position(pelota.position())
    lionel.taquito()
    assert.equals(pelota.position(), game.at(0, 5))
  }
}

describe "patear"{
    test"patear"{
        lionel.position(pelota.position())
        lionel.patear()
        lionel.position(pelota.position())
        lionel.patear()
        lionel.position(pelota.position())
        lionel.patear()
        lionel.position(pelota.position())
        lionel.patear()
        assert.equals(game.width()-1, pelota.position().x())
    }

}